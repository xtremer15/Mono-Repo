name: UI CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'UI/**'
      - '.github/workflows/ui-ci.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'UI/**'
      - '.github/workflows/ui-ci.yml'
  workflow_dispatch:

permissions:
  contents: write # Necesar pentru a putea da push pe gh-pages

jobs:
  playwright-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./UI
        
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: UI/package-lock.json
          
      - name: Install dependencies
        run: npm ci --prefer-offline
        
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
        
      - name: Run Playwright tests
        run: npx playwright test
        
      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            UI/playwright-report/
            UI/test-results/
          retention-days: 30

      - name: Publish to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: UI/playwright-report # Playwright genereaza HTML-ul aici
          destination_dir: ui-report
          keep_files: true

      - name: Send Slack Notification (Mock)
        if: always()
        run: echo "Mocking Slack Notification... Status is ${{ job.status }}"
